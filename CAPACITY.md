# CAPACITY.md - Voice2Spec 디바이스별 성능 기준선

## 1. 디바이스 등급

| 등급 | RAM | 예시 | 기본 모델 |
|---|---|---|---|
| HIGH | 8GB+ | Pixel 7 Pro, Galaxy S23+ | Qwen2.5-7B-q4 (선택) / 3B 기본 |
| MID | 6-8GB | Pixel 6a, Galaxy A54 | Qwen2.5-3B-q4 |
| LOW | 4-6GB | 보급형 | Gemma 2 2B-q4 (또는 미지원 안내) |
| MIN | <4GB | 미지원 | 앱 실행은 되지만 LLM 비활성, STT만 사용 가능 |

## 2. 성능 목표 (각 등급의 50백분위 디바이스 기준)

| 단계 | HIGH | MID | LOW |
|---|---|---|---|
| 30초 음성 STT | < 8초 | < 15초 | < 30초 |
| Refiner (300 토큰 출력) | < 5초 | < 12초 | < 25초 |
| Specifier (800 토큰 출력) | < 15초 | < 30초 | < 60초 |
| End-to-end (30초 음성 → 결과 첫 화면) | < 30초 | < 60초 | < 120초 |
| 메모리 사용량 (피크) | < 4GB | < 3GB | < 2.5GB |
| 배터리 (1회 생성) | < 1% | < 2% | < 3% |

## 3. 측정 방법
- 자동 벤치마크: 사전 정의된 5개 샘플 음성으로 측정, 결과를 `benchmark_result.json`에 기록.
- 첫 실행 시 1회 자동 측정 → 디바이스 등급 자동 분류 → 모델 추천.
- 사용자가 설정에서 재측정 가능.

## 4. 디그레이드 정책
- 측정 결과가 등급 기준의 1.5배를 초과하면 자동으로 한 등급 아래 모델 추천.
- 사용자가 더 작은 모델로 다운그레이드 가능 (Gemma 2B → whisper만 사용 → 수동 입력).

## 5. 컨텍스트 윈도우 한계
- STT 결과가 모델 컨텍스트의 50%를 초과하면 자동으로 청크 분할.
- 청크 분할 시 Specifier는 마지막 청크에 전체 요약을 포함하여 호출.

## 6. 모델 캐싱
- LLM 모델은 앱 시작 시 lazy load (홈 화면에서는 로딩 X, 녹음 종료 시점에 시작).
- 백그라운드 진입 후 5분 후 언로드 (메모리 회수).
- 다시 사용 시 워밍업 시간 (~3초) 사용자에게 표시.
