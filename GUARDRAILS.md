# GUARDRAILS.md - Voice2Spec 안전 및 품질 가드레일

## 1. 프라이버시 가드레일 (가장 중요)
- G1.1: 어떤 네트워크 요청도 사용자가 명시적으로 켜지 않으면 발생하지 않는다.
  - 검증: 앱 매니페스트에서 `android:usesCleartextTraffic="false"`, 기본 INTERNET 권한은 모델 다운로드 화면에서만 사용.
  - 자동 테스트: MockWebServer로 모든 외부 호출을 가로채 사용자 토글 OFF 상태에서 0건임을 검증.
- G1.2: 음성 파일은 사용자가 명시적으로 내보내기 전에는 앱 내부 저장소 외부로 나가지 않는다.
- G1.3: 크래시 리포트/분석 도구는 v1에 포함하지 않는다.

## 2. 모델 출력 가드레일
- G2.1: LLM 출력이 빈 문자열이거나 200자 미만이면 자동 재시도 (최대 2회).
- G2.2: LLM 출력에 시스템 프롬프트의 일부가 그대로 반복되면 (예: "당신은 ...입니다") 후처리로 제거.
- G2.3: 결과 화면 표시 전에 Validator를 반드시 통과. 실패 시 사용자에게 "다시 생성" 옵션 제공.
- G2.4: 모델이 한국어 입력에 영어로 응답하면 자동으로 한국어 재요청 (입력 언어와 출력 언어 일치 원칙).

## 3. 리소스 가드레일
- G3.1: LLM 추론 중 배터리가 15% 미만이면 사용자에게 경고 후 진행 여부 확인.
- G3.2: 디바이스 온도가 임계치 초과 시 (TemperatureMonitor API) 추론 일시 중단.
- G3.3: 음성 녹음은 최대 5분으로 제한 (그 이상은 STT 정확도와 LLM 컨텍스트가 망가짐).
- G3.4: 모델 파일 다운로드는 Wi-Fi에서만 (기본값), 사용자가 셀룰러 허용 시 명시적 동의 화면.

## 4. UX 가드레일
- G4.1: 모든 비동기 작업에 진행률 또는 무한 스피너 표시. 5초 이상 무응답 금지.
- G4.2: 에러 메시지는 "무엇이 실패했는지 + 사용자가 무엇을 할 수 있는지"를 반드시 포함.
- G4.3: 첫 실행 시 모델 다운로드 (수백 MB)는 명확한 크기/시간 안내 후 시작.

## 5. 데이터 무결성 가드레일
- G5.1: idea_*.md 파일 저장은 atomic write (임시 파일 → rename).
- G5.2: Room DB는 .md 파일에서 재구축 가능해야 함 (DB는 캐시일 뿐).
- G5.3: 삭제는 휴지통(soft delete) 30일 후 영구 삭제.

## 6. 보안 가드레일
- G6.1: 모델 파일은 다운로드 시 SHA256 검증.
- G6.2: 사용자가 입력한 외부 LLM 엔드포인트(엣지 서버 옵션)는 HTTPS 또는 로컬 네트워크(10.x, 192.168.x, Tailscale 100.x)만 허용.
- G6.3: 디버그 로그에 사용자 transcript 절대 포함 금지 (개발 빌드에서만 토글).

## 7. 가드레일 위반 시 동작
- 위반 종류에 따라 분기:
  - **HARD**: 작업 중단, 사용자에게 명시 (예: G1.1, G6.2)
  - **SOFT**: 경고 후 사용자 결정 (예: G3.1)
  - **AUTO_FIX**: 조용히 보정 (예: G2.2, G2.4)
- 모든 위반은 로컬 로그(`guardrail.log`)에 기록.
